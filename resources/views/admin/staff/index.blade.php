@extends('layouts.admin')
@section('title','Staff')
@section('page_title','Staff Management')
@section('page_description','Manage staff accounts, roles and workflow permissions.')
@section('content')
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="card"><div class="card-header d-flex align-items-center"><h3 class="card-title">Staff accounts</h3><div class="ml-auto"><a href="{{ route('admin.staff.roles.index') }}" class="btn btn-outline-secondary btn-sm mr-1">Roles & Permissions</a><a href="{{ route('admin.staff.create') }}" class="btn btn-primary btn-sm">Add staff</a></div></div><div class="card-body p-0"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Name</th><th>Username</th><th>Email</th><th>Roles</th><th>Direct permissions</th><th>Status</th><th></th></tr></thead><tbody>@forelse($staff as $member)<tr><td>{{ $member->fullname }}</td><td>{{ $member->username }}</td><td>{{ $member->email }}</td><td>@forelse($member->roles as $role)<span class="badge badge-info mr-1">{{ $role->name }}</span>@empty<span class="text-muted">None</span>@endforelse</td><td>{{ $member->permissions->count() }}</td><td><span class="badge badge-{{ $member->is_active?'success':'secondary' }}">{{ $member->is_active?'Active':'Disabled' }}</span></td><td class="text-right"><a href="{{ route('admin.staff.edit',$member) }}" class="btn btn-sm btn-outline-primary">Edit access</a></td></tr>@empty<tr><td colspan="7" class="text-center text-muted p-4">No staff accounts found.</td></tr>@endforelse</tbody></table></div>@if($staff->hasPages())<div class="p-3 border-top">{{ $staff->links() }}</div>@endif</div></div>
@endsection
